export type onSubmitFormBottomMenuFunction = (data: TArgumentsubmitBottomMenu) => void;

interface TArgumentsubmitBottomMenu {
  categoryName: string;
  categoryMoney: number;
  categoryColor: string;
}
export interface volatileData {}
